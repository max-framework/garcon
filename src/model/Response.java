package model;

/**
 * The response, usually from the server to a request.
 * If the request and the response have the same format, just
 * implement Request and Response in the same class
 */
public interface Response {
    public String getText();
}
